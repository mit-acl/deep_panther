from setuptools import setup, find_packages

setuptools.setup(
    name="Policy Compression", # Replace with your own username
    version="0.0.1",
    author="Andrea Tagliabue",
    author_email="atagliab@mit.edu",
    description="Policy compression",
    packages=find_packages(),
    python_requires=">=3.6",
)

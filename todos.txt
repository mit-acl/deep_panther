For paper:

In MyClampedUniform.m, see "Not impose constraint on the first vel ctrl point [to allow "infeasible" initial velocities due to planning in body frame and v constraints in each axis].
====
[Jan 2022] Check the Hacks






(Dec, 6)--> In Oct. Search, use max(v_max, initial_state_vel) [same for a_max]
--> bbox doesn't make too  much sense given that we keep changing the axis 

Ahora mismo, la feature se queda en el extremo del FOV (probably because the cost aossociated with the FOV constraint is ____/ (zero if constraint satisfied))
Checkout last version of MINVO (Creo que main.m da algunos errores por lack of double conversion de A)


En esta linea de PANTHER:
	 casadi::DM matrix_qy_guess(1, N_);  // TODO: do this just once?

por que uso N_?? No deberia ser el N del yaw??

Right now, octopusSolver is using the hulls sampled directly on the trajectory f(t). And the optimizer is using the hulls sampled in the spline


el field pwp (y el publicar la own trajectory) lo he comentado por ahora. Lo puedo descomentar, pero entonces debo implementar CPs2TrajAndPwp sin asumir (deg_pos=3 and deg_yaw=2)  




Add this to panther??

(in yaw_guess_generator.cpp)


    if (isnan(visibility))
    {
      std::cout << red << bold
                << "visibility is nan. This happens when Hopf fib sing has been found (i.e., bar{xi} =[0 0 -1]) "
                << reset << std::endl;
      std::cout << "Using visibility=0" << std::endl;
      visibility = 0;
    }



